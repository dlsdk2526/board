<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ina.board.mapper.BoardMapper">

	<sql id="search">
		<where>
			<if test="arr != null and arr != ''">
				<foreach collection="arr" item="item" separator="OR" open="(" close=")">
					<if test="item == 'T'.toString()">
						title like concat('%',#{keyword},'%')
					</if>
					<if test="item == 'C'.toString()">
						content like concat('%',#{keyword},'%')
					</if>
					<if test="item == 'W'.toString()">
						writer like concat('%',#{keyword},'%')
					</if>
				</foreach>
			</if>
		</where>
	</sql>

	<select id="getList" resultType="org.ina.board.domain.BoardVO">
		select * from tbl_board
		<include refid="search"></include>
		order by bno desc limit #{skip},#{perSheet}
	</select>

	<select id="getTotal" resultType="int">
		select count(bno) from tbl_board
		<include refid="search"></include>
	</select>

	<insert id="insert">
		insert into tbl_board (title, content, writer)
		VALUES
		(#{title},#{content},#{writer})
	</insert>

	<insert id="insertSelectkey">
		insert into tbl_board (title, content, writer)
		VALUES
		(#{title},#{content},#{writer})
		<selectKey keyProperty="bno" order="AFTER" resultType="long">
			SELECT last_insert_id()
		</selectKey>
	</insert>

	<select id="read" resultType="org.ina.board.domain.BoardVO">
		select * from tbl_board where bno =
		#{bno}
	</select>

	<delete id="delete">
		delete from tbl_board where bno = #{bno}
	</delete>

	<update id="update">
		update tbl_board set title = #{title}, content =
		#{content}, writer= #{writer},updatedate= now() where bno = #{bno}
	</update>
	
	<update id="updateReplyCnt">
		update tbl_board set replycnt = replycnt + #{amount} where bno = #{bno}
	</update>



</mapper>

